# Runtime intentionally uses Python standard library only.
# Keep this file so deployment and CI can install future backend dependencies
# with a stable command: python -m pip install -r requirements.txt
